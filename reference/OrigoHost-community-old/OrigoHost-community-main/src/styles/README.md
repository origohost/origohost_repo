# styles/

Extra stylesheets beyond the main `src/styles.css` entry.
Prefer Tailwind utilities and design tokens in `styles.css` first.
Only add files here for scoped, non-utility CSS (e.g. print styles, vendor overrides).
